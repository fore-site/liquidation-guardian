{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --include=dev && npm ci --prefix web --include=dev",
  "buildCommand": "NITRO_PRESET=vercel npm run build --prefix web",
  "outputDirectory": "web/.vercel/output"
}
