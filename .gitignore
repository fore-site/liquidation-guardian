# Secrets — never commit
.env
.env.local
*.key

# Agent instructions — local working notes, not project source
AGENTS.md

# KeeperHub wallet (server-side custody, but the local file authenticates signing)
.keeperhub/
wallet.json

# Node
node_modules/
dist/
*.log
npm-debug.log*
*.tsbuildinfo

# TanStack Start / nitro build artifacts
web/.nitro/
web/.output/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Agent tooling state (session memory, settings, reference screenshots)
.commandcode/
.claude/*
# …except durable run-skills, which are project assets (launch/driver code).
!.claude/skills/
.claude/skills/*
!.claude/skills/run-liquidation-guardian/
screenshots/

# Playwright smoke-test artifacts
.playwright-mcp/



